import { observer } from "mobx-react-lite"
import { Header } from "semantic-ui-react"

export default observer(function ComplexDashboard(){
    return (
        <Header>List of Complexes</Header>
    )
})